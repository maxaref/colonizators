
const players = 2;

export const game = {
  players,
  screenHeight: window.innerHeight,
  screenWidth: window.innerWidth,
  worldHeight: window.innerHeight * players,
  worldWidth: window.innerWidth * players
};